# Scriptless Workflow Test

This runtime fixture verifies an external-agent step, a LazyMind-native platform
tool step, and another external-agent step. Every step requires review after
execution so the panel exposes continue, one-step approval bypass, following-step
approval bypass, and regeneration controls. It does not declare or load any
workflow-local script.
